# AWS-Serverless-Website---Employee-Details-Query-Application

Application will download the html from the starting URL which is provided as the first argument to the program. It then parses the html finding the first reference to other absolute URLS. The application doesn't visit an already visited page and repeats the operation until no embedded references are there or numHops times.
